import phone from 'phone-task/utils/phone';
import { module, test } from 'qunit';

module('Unit | Utility | phone', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = phone();
    assert.ok(result);
  });
});
